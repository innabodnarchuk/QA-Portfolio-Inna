# 🐾 Petstore API Tests Collection

This Postman collection demonstrates basic CRUD (Create, Read, Update, Delete) operations for the Petstore Swagger API.

**Endpoints tested:**
- `GET /pet/{id}` – get a pet by ID  and by status 
- `POST /pet` – add a new pet  
- `PUT /pet` – update pet details  
- `DELETE /pet/{id}` – delete pet by ID  

**Tools:** Postman  
**Base URL:** https://petstore.swagger.io/v2  
